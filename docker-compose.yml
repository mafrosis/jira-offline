version: '3'

services:
  jiracli:
    image: mafrosis/jiracli
    build:
      context: .
    volumes:
      - ./:/app
      - ./config:/root/.config/jiracli
      - /app/.git
      - /app/jira_cli.egg-info
