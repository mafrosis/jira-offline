version: '3'

services:
  jiracli:
    image: mafrosis/jiracli
    build:
      context: .
    volumes:
      - ./:/app
      - ./config:/root/.config/jira-cli
      - /app/.git
      - /app/jira_cli.egg-info
